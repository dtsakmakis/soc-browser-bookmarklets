javascript:(async()=>{try{const stream=await navigator.mediaDevices.getDisplayMedia({video:{displaySurface:"browser"},audio:false,preferCurrentTab:true,selfBrowserSurface:"include",surfaceSwitching:"include"});const video=document.createElement("video");video.srcObject=stream;video.muted=true;await video.play();await new Promise(r=>setTimeout(r,250));const sw=video.videoWidth,sh=video.videoHeight;const pad=16,line=20,maxUrl=120;const url=location.href.length>maxUrl?location.href.slice(0,maxUrl-1)+"…":location.href;const ts=new Date().toLocaleString();const title=document.title||"(no title)";const lines=[`Time: ${ts}`,`Title: ${title}`,`URL: ${url}`];const metaH=pad*2+lines.length*line+8;const canvas=document.createElement("canvas");canvas.width=sw;canvas.height=sh+metaH;const ctx=canvas.getContext("2d");ctx.drawImage(video,0,0,sw,sh);stream.getTracks().forEach(t=>t.stop());ctx.fillStyle="#111";ctx.fillRect(0,sh,sw,metaH);ctx.fillStyle="#fff";ctx.font="14px Arial, sans-serif";ctx.textBaseline="top";lines.forEach((txt,i)=>ctx.fillText(txt,pad,sh+pad+i*line));const blob=await new Promise(r=>canvas.toBlob(r,"image/png"));await navigator.clipboard.write([new ClipboardItem({"image/png":blob})]);alert("Screenshot with metadata copied to clipboard");}catch(e){alert("Failed: "+e.message)}})();
