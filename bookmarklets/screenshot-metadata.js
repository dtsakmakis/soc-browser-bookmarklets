javascript:(async()=>{try{const note=prompt("Note / Case ID (optional):","")||"";const stream=await navigator.mediaDevices.getDisplayMedia({video:{displaySurface:"browser"},audio:false,preferCurrentTab:true,selfBrowserSurface:"include",surfaceSwitching:"include"});const video=document.createElement("video");video.srcObject=stream;video.muted=true;await video.play();await new Promise(r=>setTimeout(r,250));const sw=video.videoWidth,sh=video.videoHeight;const pad=16,line=20,maxUrl=120;const url=location.href.length>maxUrl?location.href.slice(0,maxUrl-1)+"…":location.href;const ts=new Date().toLocaleString();const title=document.title||"(no title)";const lines=[`Time: ${ts}`,`Title: ${title}`,`URL: ${url}`].concat(note?[`Note: ${note}`]:[]);const metaH=pad*2+lines.length*line+8;const canvas=document.createElement("canvas");canvas.width=sw;canvas.height=sh+metaH;const ctx=canvas.getContext("2d");ctx.drawImage(video,0,0,sw,sh);stream.getTracks().forEach(t=>t.stop());ctx.fillStyle="#111";ctx.fillRect(0,sh,sw,metaH);ctx.fillStyle="#fff";ctx.font="14px Arial,sans-serif";ctx.textBaseline="top";lines.forEach((txt,i)=>ctx.fillText(txt,pad,sh+pad+i*line));const blob=await new Promise(r=>canvas.toBlob(r,"image/png"));if(!blob)throw new Error("Failed to create PNG blob");await navigator.clipboard.write([new ClipboardItem({"image/png":blob})]);const toast=document.createElement("div");toast.textContent="📸 Screenshot with metadata copied";toast.style.position="fixed";toast.style.bottom="20px";toast.style.right="20px";toast.style.padding="10px 14px";toast.style.background="rgba(0,0,0,0.8)";toast.style.color="#fff";toast.style.fontSize="14px";toast.style.borderRadius="8px";toast.style.zIndex="999999";toast.style.fontFamily="system-ui,sans-serif";toast.style.transition="opacity 0.5s ease";document.body.appendChild(toast);setTimeout(()=>{toast.style.opacity="0";},2000);setTimeout(()=>toast.remove(),2500);}catch(e){console.error("Screenshot with metadata failed:",e);alert("Screenshot failed: "+e.message);}})();
